{
  "name": "transcend",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server && yarn start\" \"cd client && yarn start\"",
    "start:dev": "concurrently \"cd server && yarn start:dev\" \"cd client && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "client",
    "server",
    "common"
  ],
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
